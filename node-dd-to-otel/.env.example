# DataDog Browser SDK Configuration
# Required: Get from DataDog RUM Application Settings (NOT Organization API Keys)
DD_CLIENT_TOKEN=pub_your_client_token_here

# Required for RUM: Application ID from DataDog RUM Application Settings
DD_APPLICATION_ID=your_application_id_here

# Optional: DataDog site (US/EU)
DD_SITE=datadoghq.com

# Optional: Application settings
DD_SERVICE=jam-extension
DD_ENV=demo
DD_VERSION=2.1.0

# Optional: Cloudflare Worker proxy endpoint (uses official SDK proxy support)
# PROXY_ENDPOINT=https://your-worker.your-subdomain.workers.dev